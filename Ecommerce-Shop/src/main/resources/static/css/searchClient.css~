body {
    background: linear-gradient(120deg, #e0eafc 0%, #cfdef3 100%);
    min-height: 100vh;
    font-family: 'Segoe UI', 'Roboto', Arial, sans-serif;
}

.centered-container {
    min-height: 90vh;
    display: flex;
    align-items: center;
    justify-content: center;
}

.search-form {
    background: #fff;
    border-radius: 16px;
    box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.18);
    padding: 40px 32px 32px 32px;
    max-width: 1500px;
    width: 100%;
    margin: 0 auto;
    transition: box-shadow 0.3s;
}

.search-form:hover {
    box-shadow: 0 12px 40px 0 rgba(31, 38, 135, 0.22);
}

.search-form h2 {
    font-weight: 700;
    color: #2d3a4b;
    margin-bottom: 28px;
    letter-spacing: 1px;
}

.form-row {
    display: flex;
    flex-wrap: wrap;
    gap: 18px;
    margin-bottom: 18px;
}

.form-row input {
    flex: 1 1 45%;
    min-width: 0;
}

.form-control {
    border: 1.5px solid #b6c6e0;
    border-radius: 12px;
    padding: 12px 14px;
    font-size: 1rem;
    background: #f7faff;
    transition: border-color 0.2s, box-shadow 0.2s;
    margin-bottom: 0;
}

.form-control:focus {
    border-color: #5b8def;
    outline: none;
    box-shadow: 0 0 0 2px #b3d1ff55;
    background: #fff;
}

.search-btn {
    width: 100%;
    padding: 12px 0;
    background: linear-gradient(90deg, #5b8def 0%, #6dd5ed 100%);
    color: #fff;
    font-weight: 600;
    border: none;
    border-radius: 12px;
    font-size: 1.1rem;
    letter-spacing: 1px;
    box-shadow: 0 2px 8px 0 rgba(91, 141, 239, 0.10);
    cursor: pointer;
    transition: background 0.2s, box-shadow 0.2s;
}

.search-btn:hover {
    background: linear-gradient(90deg, #4a7bdc 0%, #4ecdc4 100%);
    box-shadow: 0 4px 16px 0 rgba(91, 141, 239, 0.18);
}

.results-table {
    margin-top: 30px;
    width: 100%;
    background: none;
}

.table-responsive-custom {
    border-radius: 14px;
    overflow: hidden;
    background: #fff;
    box-shadow: 0 2px 16px 0 rgba(31, 38, 135, 0.10);
    max-width: 100%;
    width: 100%;
    margin-bottom: 24px;
}

.table {
    min-width: 800px;
    margin-bottom: 0;
    background: transparent;
    border-radius: 0 !important;
    max-width: 100%;
    width: 100%;
    table-layout: auto;
    font-size: 1.01rem;
}

.table thead th {
    background: #f7faff;
    color: #2d3a4b;
    font-weight: 600;
    border-bottom: 2px solid #e0eafc;
    vertical-align: middle;
    text-align: center;
}

.table tbody td {
    vertical-align: middle;
    text-align: center;
    border-bottom: 1px solid #f0f4fa;
    padding: 10px 8px;
}

.action-buttons {
    display: flex;
    gap: 12px;
    justify-content: center;
    align-items: center;
}

@media (max-width: 600px) {
    .search-form {
        padding: 24px 8px 16px 8px;
        max-width: 98vw;
    }
    .form-row {
        flex-direction: column;
        gap: 10px;
    }
    .table {
        min-width: 600px;
    }
}

.address-cell {
    white-space: normal;
    word-break: break-word;
    max-width: 200px;
    min-width: 150px;
    text-align: left;
    font-size: 0.98em;
}